{
  "name": "albedo-wasm",
  "version": "0.1.1",
  "description": "WASM bindings for Albedo document store",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "typesVersions": {
    "*": {
      "*": [
        "./dist/index.d.ts",
        "./dist/*"
      ]
    }
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build-core": "cd albedo && zig build -Dtarget=wasm32-freestanding && cp zig-out/bin/albedo.wasm ../src/albedo.wasm",
    "build": "tsc && cp src/albedo.wasm dist/",
    "build:inline": "tsc && node scripts/inline-wasm.js",
    "test": "bun test",
    "prepublishOnly": "npm run build",
    "postinstall": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "keywords": [
    "wasm",
    "database",
    "document-store",
    "zig"
  ],
  "author": "",
  "license": "MIT"
}